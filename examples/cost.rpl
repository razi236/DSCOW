module RPL;
export Quality;
data RC = RC(Int rc);
data Quality = ResEfficiency(Int) | Int | ResCost(Int cst) | ResCapacity(Int) | Ferry | Doctor | SmokeMask | PoliceMan | Clerk | EODTech | FireFighter | FireExt | FirstAidBox;

interface DL
{
    Int check();
}
class DLD implements DL
{
    Int check()
    {
        Int i=0;
        cost(599); // consume 10 times unit
        i = check(this) after dl 21;
        return 0;

    }
}

{
    DL o = new DLD();
    Fut<Int> f;
    Fut<Int> f1;
    Fut<Int> f2;
    Int x = 0;
    x = check(o) after dl 21;
    f1 = !check(o) after f1 dl 21;
    x = check(o) after f1,f&f,f1&f dl 21;
    await f?;
    await f1?;
    await f2?;
    x = f.get;
    x = f1.get;
    x = f2.get;
    //await f1?;
    //await f2?;
    println("RplTools");
}
